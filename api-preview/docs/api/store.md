# Stores

The key-value layer, redesigned as composable **capability protocols** (not one ABC) plus composable **wrappers** and a sync/async family split. Maps to the `zarr-store` package. Source: [stores.md](../proposals/stores.md) and the tier-3 specs (`stores-api`, `stores-wrappers`, `stores-caching`, `stores-range-coalescing`, `stores-transactional`, `stores-conformance`).

## Capability protocols

::: zarr.store.protocols

## Supporting types

::: zarr.store.types

## Backends

::: zarr.store.backends

## Wrappers

::: zarr.store.wrappers

## Path helpers

::: zarr.store.path

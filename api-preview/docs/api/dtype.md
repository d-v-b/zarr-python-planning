# Data types

The Zarr dtype system and first-class ML dtypes (`bfloat16`, the `float8` variants, packed `int4`/`uint4`) via `ml_dtypes`. Maps to the `zarr-dtype` package. Source: [data-types.md](../proposals/data-types.md).

::: zarr.dtype

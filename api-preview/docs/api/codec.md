# Codecs

The sync-first codec capability bundle (`encode` / `decode` / optional `decode_into` / capability flags) plus concrete codecs. Maps to the `zarr-codec` package; also the seam where alternative [engines](engines.md) plug in. Source: [codecs.md](../proposals/codecs.md), [gpu.md](../proposals/gpu.md).

::: zarr.codec

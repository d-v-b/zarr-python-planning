# Creating arrays & groups

Explicit constructors that replace the `mode=` family. Each names its intent: `create` fails if the node exists, `create_or_overwrite` replaces it, `open_or_create` opens or creates. Source: [missing-apis.md](../proposals/missing-apis.md).

::: zarr.create_array

::: zarr.create_group

::: zarr.create

::: zarr.create_or_overwrite

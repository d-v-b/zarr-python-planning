# sync() bridge (removed in 4.0)

The global `zarr.core.sync.sync()` helper. **Replaced by** the public [`zarr.store.AsyncToSync`](../api/store.md) wrapper / `zarr.to_sync`. Source: [stores.md](../proposals/stores.md), [missing-apis.md](../proposals/missing-apis.md).

::: zarr_legacy.sync

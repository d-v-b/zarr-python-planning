# StorePath (removed in 4.0)

The `zarr.storage._common.StorePath` path abstraction. **Replaced by** the [`zarr.store.Prefixed[S]`](../api/store.md) wrapper (with `store / "sub"` navigation). Source: [stores.md](../proposals/stores.md), [missing-apis.md](../proposals/missing-apis.md).

::: zarr_legacy.storage

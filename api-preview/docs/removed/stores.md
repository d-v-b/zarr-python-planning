# Store ABC (removed in 4.0)

The monolithic `zarr.abc.store.Store` ABC and the `*ByteRequest` request objects. **Replaced by** the [`zarr.store` capability protocols](../api/store.md). Source: [stores.md](../proposals/stores.md).

::: zarr_legacy.abc_store

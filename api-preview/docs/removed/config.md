# donfig config (removed in 4.0)

The `donfig`-based `zarr.core.config.Config` and `BadConfigError`. **Replaced by** the [`zarr.config`](../api/config.md) substrate — same namespaced keys and `ZARR_*` env overrides, donfig retired. Source: [missing-apis.md §6](../proposals/missing-apis.md).

::: zarr_legacy.config
